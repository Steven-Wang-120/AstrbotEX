# 默认：文本文件在仓库里统一存成 LF，检出到工作区时按平台转换。
# 没有这个文件时 core.autocrlf=true 会让 git 每次 diff 都刷
# "LF will be replaced by CRLF" 警告，且行尾变化会污染 diff。
* text=auto eol=lf

# Windows 专用脚本：检出成 CRLF。
# .ps1 另有一条硬约束（见 PROJECT-CONVENTIONS 第 9 节）——
# 必须是 UTF-8 带 BOM，否则 PS 5.1 按 GBK 解析，中文字符串会被拆坏成语法错误。
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# 容器内执行的脚本：必须是 LF。
# CRLF 会让 shebang 变成 "/bin/sh\r"，容器启动时报 bad interpreter。
*.sh text eol=lf
Dockerfile text eol=lf
docker-entrypoint.sh text eol=lf

# 二进制文件：不做行尾转换，不参与 diff。
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.pyc binary
*.pkl binary
*.bin binary
*.so binary
*.dll binary
*.pyd binary
